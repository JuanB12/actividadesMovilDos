import 'package:flutter/material.dart';
import './Residente_Register.dart';

class ResidenteLogin extends StatelessWidget {
  const ResidenteLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}